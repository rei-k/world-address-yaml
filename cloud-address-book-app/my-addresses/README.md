# 🏠 My Addresses / 住所管理

クラウド住所帳のメイン機能。デフォルト住所、追加住所、住所履歴を管理します。

Manage your default address, additional addresses, and address history in the Cloud Address Book.

---

## 🎯 主要機能 / Key Features

### Default Address（デフォルト住所）
- **高速チェックアウト用メイン住所**: ワンクリックで配送先として使用
- **自動選択**: ECサイトでの決済時に自動的に選択される住所
- **優先表示**: 住所選択時に最初に表示

### Additional Addresses（追加住所）
- **別拠点**: サテライトオフィス、共同作業スペース
- **実家**: 帰省時の配送先
- **事務所**: ビジネス用配送先
- **海外住所**: 海外在住時の住所
- **移住先**: 引越し先の新住所（移行期間用）

### Address History（住所履歴）
- **引越タイムライン**: 過去の住所の時系列管理
- **住所ライフログ**: 各住所での居住期間・利用統計
- **自動アーカイブ**: 旧住所の自動保存

---

## 📂 ディレクトリ構成 / Directory Structure

```
my-addresses/
├── README.md                   # このファイル
├── default-address/            # デフォルト住所
│   ├── main-address.md        # メイン住所設定
│   └── quick-checkout.md      # 高速チェックアウト用
├── additional-addresses/       # 追加住所
│   ├── home.md                # 実家
│   ├── office.md              # 事務所
│   ├── branch.md              # 別拠点
│   ├── international.md       # 海外住所
│   └── relocation.md          # 移住先
└── address-history/            # 住所履歴
    ├── timeline.md            # 引越タイムライン
    └── lifelog.md             # 住所ライフログ
```

---

## 🚀 使用方法 / Usage

### デフォルト住所の設定

```typescript
import { setDefaultAddress } from '@/cloud-address-book-app/my-addresses';

await setDefaultAddress(userId, {
  country: 'JP',
  postalCode: '150-0043',
  province: '東京都',
  city: '渋谷区',
  streetAddress: '道玄坂1-2-3',
  building: 'タワーマンション',
  room: '1001'
});
```

### 追加住所の登録

```typescript
import { addAddress } from '@/cloud-address-book-app/my-addresses';

const address = await addAddress(userId, {
  label: '実家',          // ラベル: 実家, 事務所, 別拠点, etc.
  country: 'JP',
  postalCode: '600-8216',
  province: '京都府',
  city: '京都市下京区',
  streetAddress: '塩小路通烏丸西入東塩小路町',
  building: null,
  room: null
}, {
  isDefault: false,      // デフォルトに設定しない
  enableNotifications: true  // この住所への配送通知を有効化
});
```

### 住所の取得

```typescript
import { 
  getDefaultAddress,
  getAllAddresses,
  getAddressById 
} from '@/cloud-address-book-app/my-addresses';

// デフォルト住所を取得
const defaultAddr = await getDefaultAddress(userId);

// 全住所を取得（アクティブのみ）
const addresses = await getAllAddresses(userId, { activeOnly: true });

// 特定の住所を取得
const address = await getAddressById(addressId);
```

### 住所履歴の表示

```typescript
import { getAddressTimeline } from '@/cloud-address-book-app/my-addresses/address-history';

const timeline = await getAddressTimeline(userId);
// [
//   { address: '...', startDate: '2020-01-01', endDate: '2022-06-30' },
//   { address: '...', startDate: '2022-07-01', endDate: null }  // 現在の住所
// ]
```

---

## 📋 住所データモデル / Address Data Model

```typescript
interface Address {
  id: string;                    // 住所ID
  userId: string;                // ユーザーID
  label: string;                 // ラベル（実家、事務所など）
  isDefault: boolean;            // デフォルト住所フラグ
  
  // 住所情報
  country: string;               // 国コード (ISO 3166-1 alpha-2)
  postalCode: string;            // 郵便番号
  province: string;              // 都道府県/州
  city: string;                  // 市区町村
  streetAddress: string;         // 番地・丁目
  building?: string;             // 建物名
  room?: string;                 // 部屋番号
  
  // メタデータ
  pid: string;                   // Place ID (PID)
  normalized: boolean;           // 正規化済みフラグ
  verified: boolean;             // 検証済みフラグ
  verificationDate?: Date;       // 検証日時
  
  // 設定
  enableNotifications: boolean;  // 配送通知の有効/無効
  allowedCarriers: string[];     // 許可する配送業者
  
  // タイムスタンプ
  createdAt: Date;               // 作成日時
  updatedAt: Date;               // 更新日時
  lastUsedAt?: Date;             // 最終利用日時
  archivedAt?: Date;             // アーカイブ日時（null=アクティブ）
}
```

---

## 🎨 UI/UXデザイン指針 / UI/UX Design Guidelines

### 住所カード表示

```
┌────────────────────────────────────────┐
│ 🏠 デフォルト住所                       │
│                                        │
│ 〒150-0043                              │
│ 東京都渋谷区道玄坂1-2-3                  │
│ タワーマンション 1001号室                │
│                                        │
│ ✅ 正規化済み  ✅ 検証済み               │
│ 最終利用: 2日前                          │
│                                        │
│ [編集] [QRコード生成] [アーカイブ]        │
└────────────────────────────────────────┘
```

### インタラクション
- **ドラッグ&ドロップ**: 住所の順序変更
- **スワイプアクション**: モバイルで編集・削除
- **クイックアクション**: 住所カード上でQRコード生成、コピー

---

## 🔐 プライバシー機能 / Privacy Features

### 住所の暗号化
- **保存時暗号化**: AES-256-GCM
- **転送時暗号化**: TLS 1.3
- **PIDによる匿名化**: 生住所を直接公開しない

### アクセス制御
- **誰がアクセスできるか**: デフォルトは本人のみ
- **共有設定**: 友達・家族・配送業者ごとに設定
- **アクセスログ**: すべてのアクセスを記録

---

## 📊 住所の統計情報 / Address Statistics

ダッシュボードで表示される統計情報:

- 📍 **登録住所数**: 現在登録されている住所の総数
- ✅ **検証済み住所**: PID生成・VC発行済みの住所数
- 🏢 **利用サイト数**: 各住所を利用しているECサイト数
- 📦 **配送実績**: 各住所への配送回数
- 🕒 **最終利用日**: 各住所が最後に使用された日時

---

## 🌍 国際住所対応 / International Address Support

### サポートする住所形式
- 🇯🇵 **日本**: 郵便番号、都道府県、市区町村、番地
- 🇺🇸 **アメリカ**: ZIP Code, State, City, Street
- 🇬🇧 **イギリス**: Postcode, County, City, Street
- 🇨🇳 **中国**: 邮政编码、省、市、区、街道
- 🇰🇷 **韓国**: 우편번호, 도, 시, 구, 동
- その他100カ国以上対応

### 多言語表示
- 住所の現地語表示
- ローマ字表記
- 英語表記
- 配送用標準形式

---

## 🔔 通知設定 / Notification Settings

### 住所関連の通知

1. **配送通知**
   - 配送先として使用されたとき
   - 配送状況の更新

2. **セキュリティ通知**
   - 新しいデバイスからのアクセス
   - 住所情報の変更

3. **メンテナンス通知**
   - 住所の再検証が必要なとき
   - PIDの更新が推奨されるとき

---

## 🔗 関連ページ / Related Pages

- [Dashboard](../dashboard/README.md) - 住所ステータス概要
- [Shipping Tools](../shipping-tools/README.md) - 送り状生成
- [Sites Linked](../sites-linked/README.md) - 提携サイト管理
- [Security & Privacy](../security-privacy/README.md) - セキュリティ設定

---

**🌐 World Address YAML / JSON** - My Addresses Management
