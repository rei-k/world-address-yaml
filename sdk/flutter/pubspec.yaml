name: vey_sdk
description: Flutter SDK for World Address YAML - Address validation and formatting
version: 1.0.0
homepage: https://github.com/rei-k/world-address-yaml

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
